<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fah_fee">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ContactActivity"></activity>
        <activity android:name=".HowToActivity" />
        <activity android:name=".WalletActivity" />
        <activity android:name=".DrawTimeActivity" />
        <activity android:name=".NumbersActivity" />
        <activity android:name=".EditProfilePageActivity" />
        <activity android:name=".HomePageActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".BetTimeActivity" />
        <activity android:name=".LastStepActivity" />
        <activity android:name=".PlaceBetActivity" />
        <activity android:name=".Activities.Registration_Login.ProfilePageActivity" />
        <activity android:name=".Activities.Registration_Login.PinRetrievalPageActivity" />
        <activity android:name=".Activities.Registration_Login.RegistrationActivity" />
        <activity android:name=".Activities.Registration_Login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>